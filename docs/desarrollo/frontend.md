# 🎨 Documentación de Frontend (Interfaz de Usuario)

Detalle técnico de la aplicación cliente y visualización de datos.

## 1. Stack Tecnológico
* **Framework:** React v. (completar)
* **Herramienta de Construcción:** (ej. Vite / Webpack)
* **Gestor de Estado:** (ej. Redux / Zustand / Context API)
* **Librería de Componentes:** (ej. Tailwind CSS / Material UI)

## 2. Visualización Geográfica
* **Librería de Mapas:** (ej. Leaflet / OpenLayers / Mapbox GL JS)
* **Consumo de Capas:** (Indicar si se consumen GeoJSON directos de la API o vía servicios OGC como WMS/WFS).

## 3. Integración con el API
* **URL Base de Producción:** `http://10.40.111.131:XXXX/api/v1/`
* **Manejo de Autenticación:** (ej. Portador de Token Bearer en Headers).

## 4. Estructura de Carpetas Clave
* `/src/components`: Componentes reutilizables.
* `/src/hooks`: Lógica de consumo de datos.
* `/src/maps`: Configuraciones específicas de los visores.

## 5. Comandos de Desarrollo
* **Instalar dependencias:** `npm install` o `yarn`
* **Compilar para producción:** `npm run build`
